// C:\Users\mhncity\ruewio\vite.config.js

import { defineConfig } from 'vite';

// 설정을 포함하는 객체를 기본 내보내기(export default) 해야 합니다.
export default defineConfig({
  // 여기에 필요한 Vite 구성 옵션을 추가합니다.
  // 예: plugins: [...], base: '/', build: { ... }
});
